
<h1>注文情報確認画面</h1>
<thead>
	<tr>商品名</tr>
	<tr>単価(税込)</tr>
	<tr>数量</tr>
	<tr>小計</tr>
</thead>

<%= form_for @order,url: {action: 'create'} do |f| %>
支払い方法
<%= f.hidden_field :payment_method %>
<%= @order.payment_method %>

お届け先
<%= f.hidden_field :selected_address %>
<%= @order.selected_address %>

<%= f.submit "購入を確定する" %>
<% end %>
