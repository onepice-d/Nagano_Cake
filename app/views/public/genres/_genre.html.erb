<table>
	<thead>
	<th>ジャンル検索</th>
	</thead>

	<tbody>
		<% @genres.each do |genre| %>
		<tr>
			<td>
				<%= link_to  public_items_path, sort: genre.id do%>
				<%= genre.name %>
				<% end %>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>