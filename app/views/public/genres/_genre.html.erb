<table class="table">
	<div  class="border">
	<thead>
	<th scope="col">ジャンル検索</th>
	</thead>

	<tbody>
		<% @genres.each do |genre| %>
		<tr>
			<td>
				<%= link_to genre.name, public_items_path(params[:sort]),　sort: genre.id  %>
			</td>
		</tr>
		<% end %>
	</tbody>
	</div>
</table>